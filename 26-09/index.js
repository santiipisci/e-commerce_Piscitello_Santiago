let opcion = prompt("¿Qué deseas hacer con la lista? 1. Ver lista - 2. Agregar un elemento - Invertir lista");

let lista = [1, 2, 3, 4, 5];

switch (opcion){
    case "1":
        console.log(lista);
        break;
    case "2":
        let item = prompt("Escribe el elemento a agregar");

if (lista.includes(item)){
    
}else{

}

        opcion.includes(item)
        break;
    case "3": 
    console.log(opcion.reverse());
    break;
}


